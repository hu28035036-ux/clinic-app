**Findings**

1. **HIGH** [app/routers/ai_commands_router.py](/C:/Users/user/Desktop/새%20폴더/병원예약관리/병원예약관리/app/routers/ai_commands_router.py:645)  
   `create_leave` approve 경로가 stored status를 `needs_approval`로 강제하지 않습니다. 현재는 `executed/rejected/failed`만 409로 막고, `intent == "create_leave"`이면 `resolved_json` 필드만 보고 바로 `execute_approved_leave()`로 들어갑니다. appointment 경로에는 [line 717](/C:/Users/user/Desktop/새%20폴더/병원예약관리/병원예약관리/app/routers/ai_commands_router.py:717)의 `result.status == needs_approval` Gate 1이 있는데 leave에는 대응 검사가 없습니다.  
   영향: `parsed`, `patient_selection_required`, `validation_failed` 같은 비종결 상태의 leave command가 필드만 갖추면 승인 실행될 수 있어 AI_SAFETY_POLICY의 “사용자 승인 상태에서만 실행” 보장이 leave intent에서 약합니다.

2. **MEDIUM** [README.md](/C:/Users/user/Desktop/새%20폴더/병원예약관리/병원예약관리/README.md:164)  
   README 상단 현재 버전은 `v1.3.5`로 갱신됐지만, “버전 기록” 표의 최신 행은 여전히 `v1.3.3`입니다. 배포 문서 정합성 체크 항목 기준으로는 README 내부에 `v1.3.5` 릴리스 행이 빠져 있어 사용자/검토자가 최신 변경을 README에서 확인할 때 불일치가 남습니다.

**검토 결과**

- 반영됨: terminal stored-state gate는 `executed/rejected/failed`를 409로 차단합니다.
- 반영됨: `rejected -> approve`, `executed -> approve` 테스트가 추가됐습니다.
- 반영됨: `APP_VERSION`, `VERSION.txt`, `versions/INDEX.txt`, README 상단 버전은 `v1.3.5`입니다.
- 반영됨: `scripts/check_db_path.py` unknown path는 `return 1`로 변경됐습니다.
- 보류/미흡: leave approve의 `needs_approval` stored-state Gate 1 누락, README 버전 기록 표 누락.

테스트는 이 세션이 `read-only` sandbox라 재실행하지 못했습니다. 코드/문서 diff 기반 리뷰입니다.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  도수치료예약 (병원 예약 관리 프로그램)
  v1.3.2  (2026-04-30)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[빌드 정보]
  실행 플랫폼    : Windows x64
  배포 방식       : PyInstaller onedir
  Python 런타임  : 3.12 (내장)
  DB             : SQLite

[주요 변경 (v1.3.2) — 직원 관리 강화]

  🧑‍⚕️ 치료사 휴무 — 연차/월차 분류 (m009)
     · 휴무 관리 모달에서 치료사별 연차/월차 선택
     · employee_leaves.leave_kind 컬럼 추가 (default 'annual')
     · /api/employee-leaves (POST/GET/bulk-set) 응답에 leave_kind 포함
     · 캘린더 이벤트 타이틀에 "(연차/월차)" 표시
     · 동일 (employee_id, leave_date) 재요청 시 payload 값 갱신 (회귀 수정)

  🧑‍⚕️ 치료사 정보 — 입사일 (m010)
     · 치료사 수정 모달에 "입사일" 입력칸 추가
     · 자동변환: 20200101 → 2020-01-01 (생년월일과 동일 패턴)
     · 직원 목록 시트 치료사 테이블에 "입사일" 컬럼 추가
     · 의사 테이블에는 표시 안 함 (can_eswt/can_manual 과 동일 분기)
     · employees.hire_date 컬럼 추가 (nullable)

  ✅ 회귀 방지 테스트 +8건 (132 passed)
     · test_employee_can_manual_contract — can_manual 필드 보호
     · test_employee_leave_kind — leave_kind 4종 시나리오
     · test_employee_hire_date — hire_date 라운드트립

  ⚙️ 마이그레이션 정책
     · DB 컬럼 추가만 (DROP/DELETE 없음, 멱등성 보장)
     · 기존 데이터 100% 보존, 자동 업데이트 시 다음 부팅 때 자동 적용

[이전 주요 변경]
  v1.3.1 — 보안 + 안정성 패치 (자동업데이트 hang, 인증 누수, DB 복원, SMS 비밀)
  v1.3.0 — AI/RAG 1차 통합 (AI 도우미 탭 + SMS 초안/점검 + PII 보호)
  v1.2.18 — 문자나라 발송 결과 오판정 수정 + 통계탭 엑셀
  v1.2.17 — 점심시간 + 도수치료 엑셀 + 신규 환자 중복 차단
  v1.2.16 — 문자나라 설정 저장 시 비밀번호 초기화 버그 수정
  v1.2.15 — 문자나라 SMS 한글 인코딩 UTF-8 → CP949
  v1.2.14 — 예약 탭 우측 빈 공간 근본 해결
  v1.2.10~13 — 예약 표 반응형 레이아웃 시도

[자동 업데이트]
  관리자 → ≡ 시스템 → [업데이트 확인] → [다운로드] → [지금 설치]
  설치 후 Ctrl+Shift+R (브라우저 캐시 갱신)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

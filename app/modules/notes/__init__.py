"""modules.notes — 메모 도메인 후보 구조 (19-7 + 20-2 F-12).

19-7 본 세션 범위:
  - **rules.py** : 메모 분류 도메인 규칙 (환자별 메모 vs 예약별 메모 / 지속 메모 vs
    당일 메모 후보 분류 + PII 마스킹 helper).

20-2 (post-19-P / F-12) 추가:
  - **service.py** : Patient.memo / Appointment.memo 통합 read/write 헬퍼
    (사용자 §4-B 권장값 (a) — rules.py 위에 service 신설).
  - 신규 저장소 / 신규 router / DB schema 변경 ⊥.

NOTE: 현재 구현된 메모 유형 (사실 — *단정* ⊥):
  - **환자별 메모** (`Patient.memo`) — 환자 모달 / 검색 결과에 노출되는 *영구 누적*
    메모. ``api.py:update_patient_memo`` (line 1444) 가 단독 저장.
  - **예약별 메모** (`Appointment.memo`) — 예약 1건 단위 메모. 취소 시
    `[취소] {memo}` 자동 추가.
  - 기존 코드 분류상 위 두 종류 만 명확히 구현 — *당일 메모 vs 지속 메모* 같은 별도
    분류는 *기능적 매핑* 으로만 표현 (단정 ⊥).

NOTE: 후속 검토 (19-7 범위 외):
  - 별도 ``Note`` 테이블 (m014+) — 다중 메모 / 작성자 / 작성 시각 분리.
  - 메모 카테고리 (의료 / 일정 / 보조 정보).
  - 메모 검색 / 필터 / 통계.
  → 본 19-7 시점에는 *현재 기능* 만 분류 정합. 부재 항목은 후속 검토 분류.

# COMPAT: 기존 ``Patient.memo`` / ``Appointment.memo`` 동작 그대로. 본 패키지는
#         *분류 / 마스킹 helper* 만 — 라우터 / DB / 응답 dict 변경 ⊥.

# SAFETY: 메모 안에 PII (전화 / 진단명 / 주소) 가 포함될 수 있음. 로그 / AI prompt
#         / 진단 출력에는 *항상 마스킹*. 운영 응답 dict 는 *기존 평문 그대로*
#         (UI 가 평문 메모 필요).

# RISK: 메모 분류 (당일 / 지속 / 환자 / 예약) 를 *DB 단위로 분리* 하는 것은 19-P 범위
#       외 (m014+ 후속 — 19-P-2 §3-2 정합). 본 19-7 은 *현재 분류 명시* 만.
"""
